name: betto_schema
description: Provides functionality for validating data against a schema.
version: 0.1.0
repository: https://github.com/bettongia/schema

environment:
  sdk: ">=3.10.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  characters: ^1.3.0
  collection: ^1.19.1
  intl: ^0.20.2
  uuid: ^4.5.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
